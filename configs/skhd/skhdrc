# SpaceHopper SKHD 配置 —— lite / 无 yabai 版（4 桌面布局）
# 安装到：~/.config/skhd/skhdrc
# 切桌面靠 macOS 自带「切换到桌面 N」+ SkyLight 只读查询，不需要关 SIP。
#
# 注意：
#   - skhd 配置语法不支持自定义变量；但命令部分由 shell 执行，故 $HOME 可用。
#   - 若仓库不在 $HOME/Documents/GithubRepo/SpaceHopper，请改下面的路径。
#
# 桌面布局（示例）：
#   桌面 1 = 聊天(飞书)   桌面 2 = 编辑器(Cursor)   桌面 3 = 浏览器(Chrome)   桌面 4 = 音乐
#
# 前提（一次性，均不关 SIP）：
#   1) 系统设置 → 键盘 → 键盘快捷键 → 调度中心：勾选「切换到桌面 1/2/3/4」(Ctrl+数字)
#   2) 给 skhd 开「辅助功能」权限

# ===== 桌面跳转（带 toggle：再按一次回到上一个桌面）=====
ctrl - k : zsh "$HOME/Documents/GithubRepo/SpaceHopper/bin/app_shortcuts/switch_to_chat.sh"
ctrl - v : zsh "$HOME/Documents/GithubRepo/SpaceHopper/bin/app_shortcuts/switch_to_vscode.sh"
ctrl - e : zsh "$HOME/Documents/GithubRepo/SpaceHopper/bin/app_shortcuts/switch_to_chrome.sh"
ctrl - x : zsh "$HOME/Documents/GithubRepo/SpaceHopper/bin/app_shortcuts/switch_to_music.sh"

# Alt + Space -> 主切换器（当前桌面 <-> 桌面 2 来回 toggle）
alt - space : zsh "$HOME/Documents/GithubRepo/SpaceHopper/bin/space_jump.sh"
